import styled from 'styled-components';

export const SNavbar = styled.nav`
  padding: 1.8rem 1.2rem;
  background-color: var(--clr-black);
  display: flex;
  align-self: start;
  align-items: center;
  gap: 1.5rem;
  position: relative;
`;

export const SNavLogo = styled.nav`
  width: 22rem;
  margin-right: auto;
`;

export const SHelp = styled.nav`
  width: 3.2rem;
`;

export const SUser = styled.button`
  width: 4rem;
`;
