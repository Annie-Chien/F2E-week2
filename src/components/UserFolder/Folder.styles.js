import styled from 'styled-components';

export const SFolder = styled.div`
  height: 100%;

  header {
    padding-bottom: 1rem;
    display: flex;
    gap: 20%;
    align-items: center;
  }

  h5 {
    font-size: var(--fz-h5);
    color: var(--clr-secondary);
  }
`;
